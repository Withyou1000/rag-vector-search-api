# 全局 AGENTS.md 默认规则

默认使用中文交流与输出。

## 中文优先

- 新生成的 README、开发文档、使用说明、学习材料、注释说明默认使用简体中文。
- 除非用户明确要求英文，禁止把新文档默认写成英文。
- 如需保留专业术语，优先采用“中文 + English term”的写法，例如“输出契约（Output Contract）”。
- 修改现有英文项目时，如果用户没有要求整体翻译，可以保留原有英文标识、API 名、代码变量名、文件名和命令。

## 代码生成规则

- 生成或修改代码时，必须为新增函数、复杂分支、异常处理、外部接口调用、测试逻辑添加简体中文注释。
- 注释应解释“为什么这样做”或“这段逻辑负责什么”，避免写无意义注释。
- 简单自明的导入、赋值、返回语句可以不加注释，但一段新增业务逻辑不能完全没有注释。
- 新增示例代码、教学代码、脚本代码时，优先让变量名清晰，并配合中文注释帮助理解。

## 文档与说明

- 新增或修改 README、教程、开发说明、使用说明时，默认用简体中文。
- 面向初学者的材料要尽量写清楚概念、用途、例子和运行方式。
- 命令、路径、环境变量、API 名、类名、函数名保持原样，不强行翻译。

## GitHub 默认配置

- 默认 GitHub owner：Withyou1000
- 新项目默认仓库命名：使用项目英文语义名，全部小写，单词用 `-` 分隔。
- 本项目默认仓库：`Withyou1000/rag-vector-search-api`
- 如果用户说“上传到仓库”但没有指定仓库，默认使用“默认 GitHub owner/项目英文语义名”。
- 如果 MCP 没有创建仓库能力，且远程仓库不存在，需要提醒用户先在 GitHub 创建空仓库。
